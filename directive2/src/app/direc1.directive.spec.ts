import { Direc1Directive } from './direc1.directive';

describe('Direc1Directive', () => {
  it('should create an instance', () => {
    const directive = new Direc1Directive();
    expect(directive).toBeTruthy();
  });
});
